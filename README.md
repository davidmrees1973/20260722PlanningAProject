Summary:
1. Set up SSH Key on local machine
2. On github added the .pub ssh key
3. Created new repo on Github
4. Clone the repo via ssh url (not https).

Commands to learn:
- CLI:
cd
ls
mkdir
pwd
cat

-Git:
git clone

